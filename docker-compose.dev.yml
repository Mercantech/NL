# Valgfri override oven på docker-compose.yml — eksponér port lokalt.
# Kræver: docker network create dokploy-network
#
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build

services:
  frontend:
    ports:
      - "${LOCAL_PORT:-8080}:80"
